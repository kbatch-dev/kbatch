apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kbatch-server.fullname" . }}-test-connection"
  labels:
    {{- include "kbatch-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kbatch-server.fullname" . }}:{{ .Values.app.service.port }}']
  restartPolicy: Never
